version: '3.3'

services:
    api:
        image: stocksimulator-api:1.0
        build: ./api
        ports:
            - '5000:5000'

    database:
        image: stocksimulator-database:1.0
        build: ./data
        ports:
            - '5432:5432'
        volumes:
            - dbdata:/var/lib/postgresql/data

volumes:
    dbdata:
